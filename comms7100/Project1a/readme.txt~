This projects is written in fortran and compiled and tested on a linux machine.
There is a makefile attached for compiling and then it is run with one commandline argument of the data file.
If the program is run without a usable argument the program will exit. 

The format for running the program is:
./Project1a [filename] 

Note that this project makes use of gnuplot.
If you do not have gnuplot installed you must do that before this will run.
